from setuptools import setup

setup(
  name = "mython",
  version = "0.1.0",
  description = "My first python package",
  license = "MIT",
  auther = "ymat2",
  python_requires = ">=3.6",
  scripts = ["mython/mython",]
)
